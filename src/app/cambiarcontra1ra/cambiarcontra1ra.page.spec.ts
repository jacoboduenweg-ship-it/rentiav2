import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Cambiarcontra1raPage } from './cambiarcontra1ra.page';

describe('Cambiarcontra1raPage', () => {
  let component: Cambiarcontra1raPage;
  let fixture: ComponentFixture<Cambiarcontra1raPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Cambiarcontra1raPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
